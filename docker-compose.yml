services:
  api:
    build: .
    container_name: mahjong-api
    ports:
      - "8000:8000"

  web:
    build: ./frontend
    container_name: mahjong-web
    ports:
      - "5173:80"
    depends_on:
      - api
